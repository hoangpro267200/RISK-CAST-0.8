import ResultsPage from './pages/ResultsPage';

export default function App() {
  // Results page is served at /results route by backend
  // No routing needed - just render ResultsPage directly
  return <ResultsPage />;
}
