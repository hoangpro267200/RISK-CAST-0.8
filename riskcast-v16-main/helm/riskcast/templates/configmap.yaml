apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "riskcast.fullname" . }}-config
  labels:
    {{- include "riskcast.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
